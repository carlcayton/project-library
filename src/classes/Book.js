class Book {
   constructor(id, title, author, stars, pageCount, pageCompleted) {
      this.id = id;
      this.title = title;
      this.author = author;
      this.stars = stars;
      this.pageCount = pageCount;
      this.pageCompleted = pageCompleted;
   }
}

export default Book